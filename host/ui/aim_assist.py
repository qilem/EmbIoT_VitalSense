# AimAssistOverlay has been merged into CompanionWindow as an inline signal bar.
# This file is kept as an empty stub so existing imports don't break.

class AimAssistOverlay:
    """Stub — functionality moved to CompanionWindow._signal_bar."""
    def show(self): pass
    def hide(self): pass
    def update_signal(self, signal: float): pass
    def isVisible(self): return False
